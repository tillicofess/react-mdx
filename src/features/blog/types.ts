export type BlogLinkType = {
    title: string;
    image?: string;
    createdAt: string;
  };
  
  export type BlogType = {
    id: string;
    cover_image: string;
    title: string;
    created_at: string;
  };